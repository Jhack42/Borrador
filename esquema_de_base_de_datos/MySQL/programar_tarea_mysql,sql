CREATE DATABASE programar_tarea_mysql;

USE programar_tarea_mysql;
drop database programar_tarea_mysql;

CREATE TABLE tareas_programadas (
    id INT AUTO_INCREMENT PRIMARY KEY,
    fecha_ejecucion DATETIME,
    descripcion VARCHAR(255)
);
INSERT INTO tareas_programadas (fecha_ejecucion, descripcion) VALUES ('2024-01-24 12:12:00', 'Enviar correo electrónico');





CREATE TABLE tareas_programadas (
	id INT AUTO_INCREMENT PRIMARY KEY,
	fecha DATE NOT NULL,
	hora TIME NOT NULL,
	tarea_nombre VARCHAR(255) NOT NULL,
	script_path VARCHAR(255) NOT NULL,
	email_remitente VARCHAR(255) NOT NULL,
	asunto VARCHAR(255) NOT NULL,
	email_destinatario VARCHAR(255) NOT NULL
	);
ALTER TABLE tareas_programadas ADD destinatario VARCHAR(255) NOT NULL;


CREATE TABLE tareas_programadas (
    id INT AUTO_INCREMENT PRIMARY KEY,
    fecha DATE NOT NULL,
    hora TIME NOT NULL,
    tarea_nombre VARCHAR(255) NOT NULL,
    script_path VARCHAR(255) NOT NULL
);
